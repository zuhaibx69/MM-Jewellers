@model IEnumerable<MZ_Jewellers.Models.Jeweller>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.jeweller_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.jeweller_password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.jeweller_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.jeweller_password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.jeweller_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.jeweller_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.jeweller_id })
        </td>
    </tr>
}

</table>
